{
  "name": "transcendence",
  "version": "1.0.0",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --require tsconfig-paths/register server.ts",
    "build": "tsc"
  },
  "dependencies": {
    "tsconfig-paths": "^4.0.0",
    "express": "^4.19.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "ts-node-dev": "^2.0.0"
  }
}
